// import { Router } from 'express';
// import authenticator from '../middleware/authenticator';
// import * as controller from '../controller/role';

// const route = Router();

// route.post('/create', authenticator, controller.createRole);

// route.get('/get_self/:id', authenticator, controller.getOne);

// route.put('/update/:id', authenticator, controller.update);

// export default route;
